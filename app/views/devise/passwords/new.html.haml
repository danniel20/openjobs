%h1.h3.mb-3.font-weight-normal.title{style: "text-align: center"} Esqueceu sua senha?
= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
  = f.error_notification
  .form-inputs
    = f.input :email, required: true, label: false, autofocus: true, input_html: { autocomplete: "email" }, placeholder: 'Email'
  .form-actions
    = f.button :submit, "Enviar instruções", class: 'btn btn-primary w-100'
  %hr
  = render "devise/shared/links"
